# example/pubspec.yaml
name: flutter_element_example
environment:
  sdk: "^3.6.0"
dependencies:
  flutter:
    sdk: flutter
  flutter_element_plus:
    path: ../
  get: ^4.6.6
  logger: ^2.5.0
  url_launcher: ^6.3.1
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
flutter:
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/image/
